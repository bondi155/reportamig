import React from 'react';

import ModeloRamos from '../components/ModeloRamos.js';

const RamoCaucion = () => {
  return (
    <ModeloRamos
      titulo={'Ramo Caución'}
      id_arch={9}
      NombreArchivo={'ramo_caucion'}
    />
  );
};

export default RamoCaucion;
